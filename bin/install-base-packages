#!/bin/bash

sudo add-apt-repository ppa:h-realh/roxterm
sudo apt-get update

sudo apt install vim curl tree htop zsh i3 lightdm roxterm xautolock pasystray scrot gimp sxiv 

#wget https://github.com/robbyrussell/oh-my-zsh/raw/master/tools/install.sh -O - | zsh
#chsh -s `which zsh`
